[project]
name = "construction-legal-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.0.7",
    "pymupdf>=1.26.6",
    "redis",
    "fastapi[standard]",
]
